
# Dolt database files (added by bd init)
.dolt/
*.db

# Local Pi materialized remote reference caches
.pi/remote-refs/
.pi/web-refs/
.pi/web-searches/

# Beads runtime state (per-machine, not canonical project data)
.beads/push-state.json

# Local transient policy/plugin artifacts
.cupcake/
.opencode/
